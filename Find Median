
class Solution
{
public:
	public:
		int find_median(vector<int> v)
		{
		    // Code here.
		    sort(v.begin(),v.end());
		    int n=v.size();
		    if(n%2==0) return (double(v[(n-1)/2]+v[n/2])/2.0);
		    else return(double(v[n/2]));
		}
};
